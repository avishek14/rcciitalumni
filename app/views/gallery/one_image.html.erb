<%= link_to "<< back", gallery_gallery_path %>

<%= image_tag @gal_one.gallery_image, alt: @gal_one.caption %>
<p><%= @gal_one.caption %></p>
<p><%= @gal_one.desc %></p>

<% if is_admin? %>
	<%= link_to "Delete", admin_delete_image_path( id: @gal_one.id) %>
<% end %>