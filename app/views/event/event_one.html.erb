<div class = 'event-one-wrapper'>
<h2><%= @e_one.title %></h2>
<h6><%= @e_one.date_on.strftime "%d/%m/%Y" %></h6>
<div class = 'desc'><%= txtl @e_one.desc %></div>
<br/>
<% if is_admin? %>
	<%= link_to "Delete", admin_delete_event_path(id: @e_one.id), class: 'button' %>
<% end %>
</div>